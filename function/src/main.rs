// this is mainly about the function in rust =D
//

mod basic_func;
mod if_match;
mod loop_iteration;
mod return_value;

use basic_func::function_basic;
use if_match::if_match_basic;
use loop_iteration::loop_iterator_basic;
use return_value::return_basic;

fn main() {
    //    if_match_basic();
    //loop_iterator_basic();
    //function_basic();
    return_basic();
}
